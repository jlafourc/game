<%= simple_form_for(@equipe) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :nom_club %>
    <%= f.input :nom_competition %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
